[request && traverse(request.getQueryParams(), 'tx_news_pi1/news') > 0]

	// Einbindung Breadcrumb -> angepasste Version in Sub-Extension einbinden (falls noetig)
	navigation.breadcrumb.dataProcessing.20 = Ps14\KistNews\DataProcessing\BreadcrumbProcessor
[END]

config.recordLinks.txNews {
	typolink {
		# Detail page
		parameter = 248
		additionalParams.data = field:uid
		additionalParams.wrap = &tx_news_pi1[controller]=News&tx_news_pi1[action]=detail&tx_news_pi1[news]=|
	}
}

